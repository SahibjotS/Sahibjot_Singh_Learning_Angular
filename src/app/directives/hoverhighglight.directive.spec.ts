import { HoverhighglightDirective } from './hoverhighglight.directive';

describe('HoverhighglightDirective', () => {
  it('should create an instance', () => {
    const directive = new HoverhighglightDirective();
    expect(directive).toBeTruthy();
  });
});
